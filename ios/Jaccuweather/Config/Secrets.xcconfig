// Empty placeholders so the project opens without extra files.
// Optional pollen keys and the NWS User-Agent are edited in the app (Keychain).
// You can still fill these locally. Never commit real keys or a personal email.
GOOGLE_POLLEN_API_KEY =
TOMORROW_API_KEY =
NWS_USER_AGENT =
