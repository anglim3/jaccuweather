--- a/public/app.js
+++ b/public/app.js
@@ -4811,4 +4811,4 @@
-                        <span class="text-yellow-400 text-xs">↑</span> <span class="text-sm font-semibold">${data.daily.sunrise && data.daily.sunrise[i] ? formatTime12Hour(new Date(data.daily.sunrise[i])) : 'N/A'}</span>
+                        <span class="text-yellow-400 text-xs">↑</span> <span class="text-sm font-semibold">${data.daily.sunrise && data.daily.sunrise[dayIndex] ? formatIsoLocalClock(data.daily.sunrise[dayIndex]) : 'N/A'}</span>
                     </div>
                     <div class="text-white mt-0.5">
-                        <span class="text-orange-400 text-xs">↓</span> <span class="text-sm font-semibold">${data.daily.sunset && data.daily.sunset[i] ? formatTime12Hour(new Date(data.daily.sunset[i])) : 'N/A'}</span>
+                        <span class="text-orange-400 text-xs">↓</span> <span class="text-sm font-semibold">${data.daily.sunset && data.daily.sunset[dayIndex] ? formatIsoLocalClock(data.daily.sunset[dayIndex]) : 'N/A'}</span>
